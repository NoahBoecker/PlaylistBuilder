Make a config file that looks like this named .config in the base directory.
```
[DEFAULT]
username = my_spotify_username
client_id = my_client_id
client_secret = my_client_secret
redirect_url = my_redirect_url
```